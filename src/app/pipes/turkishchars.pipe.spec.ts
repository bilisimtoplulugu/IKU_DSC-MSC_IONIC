import { TurkishcharsPipe } from './turkishchars.pipe';

describe('TurkishcharsPipe', () => {
  it('create an instance', () => {
    const pipe = new TurkishcharsPipe();
    expect(pipe).toBeTruthy();
  });
});
